<% cache_if edition.topic_ids.empty?, taggable_topics_cache_digest do %>
  <fieldset class="edition-topic-fields">
    <%= form.label :topic_ids, 'Policy areas', required: true %>
    <%= form.select :topic_ids, options_for_select(taggable_topics_container, edition.topic_ids), {}, multiple: true, class: 'chzn-select', data: { placeholder: "Choose policy areas..."} %>
  </fieldset>
<% end %>
